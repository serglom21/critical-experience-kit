# Task fixture: checkout-js (BEFORE)

Stands in for a customer repo at the state `gap/fixtures/observed-customer.example.json`
describes: 5 of 7 checkout spans instrumented, one of them under a drifted name,
a boolean outcome, and `cart.value` stringified.

The eval task is: give an agent this repo plus `checkout-SPEC.md`, and grade the
result against `checkout-RUBRIC.json`.
