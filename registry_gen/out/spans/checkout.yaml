# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Checkout (checkout)
#
# IMPORTANT: `requirement_level` on a SPAN group is documentation only.
# `weaver registry live-check` is sample-driven and has no span lookup —
# it will never report a missing span or a missing span attribute. Journey
# completeness is enforced by gap/analyze.py against Sentry. See
# BUILD-PLAN.md §0.
#
# span_kind and the `sentry_op` hints below are DEFAULTS derived from each
# step's `surface`. Nothing in a journey definition distinguishes an
# outbound call (client) from handling an inbound one (server) — review.

groups:

  - id: span.checkout.cart_reviewed
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 1 of the Checkout journey: cart_reviewed.
    note: |
      journey: checkout
      journey_step: 1
      journey_role: root
      surface: browser
      sentry_op: ui.action
      sentry_span_name: "checkout"
      impact: normal
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `cart_reviewed` on this span.
      - ref: checkout.outcome
        requirement_level:
          conditionally_required: >-
            Required before this span ends. Initialise to `abandoned` and overwrite on a
            terminal event.
      - ref: cart.value
        requirement_level: required
      - ref: user.plan_tier
        requirement_level: recommended
      - ref: payment.method
        requirement_level: recommended
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.shipping_submitted
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 2 of the Checkout journey: shipping_submitted.
    note: |
      journey: checkout
      journey_step: 2
      journey_role: step
      surface: browser
      sentry_op: ui.action.click
      sentry_span_name: "checkout.shipping_submitted"
      impact: normal
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `shipping_submitted` on this span.
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.payment_submitted
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 3 of the Checkout journey: payment_submitted.
    note: |
      journey: checkout
      journey_step: 3
      journey_role: step
      surface: browser
      sentry_op: ui.action.click
      sentry_span_name: "checkout.payment_submitted"
      impact: important
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `payment_submitted` on this span.
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.payment_authorized
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 4 of the Checkout journey: payment_authorized.
    note: |
      journey: checkout
      journey_step: 4
      journey_role: step
      surface: node
      sentry_op: function
      sentry_span_name: "checkout.payment.authorize"
      impact: critical
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `payment_authorized` on this span.
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.inventory_reserved
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 5 of the Checkout journey: inventory_reserved.
    note: |
      journey: checkout
      journey_step: 5
      journey_role: step
      surface: node
      sentry_op: function
      sentry_span_name: "checkout.inventory.reserve"
      impact: important
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `inventory_reserved` on this span.
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.order_created
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 6 of the Checkout journey: order_created.
    note: |
      journey: checkout
      journey_step: 6
      journey_role: step
      surface: node
      sentry_op: function
      sentry_span_name: "checkout.order.create"
      impact: critical
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `order_created` on this span.
      - ref: order.value
        requirement_level: required
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.

  - id: span.checkout.confirmation_viewed
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 7 of the Checkout journey: confirmation_viewed.
    note: |
      journey: checkout
      journey_step: 7
      journey_role: terminal
      surface: browser
      sentry_op: ui.action.click
      sentry_span_name: "checkout.confirmation_viewed"
      impact: important
    attributes:
      - ref: checkout.id
        requirement_level: required
      - ref: checkout.step
        requirement_level: required
        note: MUST be `confirmation_viewed` on this span.
      - ref: payment.decline_reason
        requirement_level:
          conditionally_required: >-
            If the journey outcome is one of: abandoned, failed, rejected.
