# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Health Probe (health_probe)
#
# IMPORTANT: `requirement_level` on a SPAN group is documentation only.
# `weaver registry live-check` is sample-driven and has no span lookup —
# it will never report a missing span or a missing span attribute. Journey
# completeness is enforced by gap/analyze.py against Sentry. See
# BUILD-PLAN.md §0.
#
# span_kind and the `sentry_op` hints below are DEFAULTS derived from each
# step's `surface`. Nothing in a journey definition distinguishes an
# outbound call (client) from handling an inbound one (server) — review.

groups:

  - id: span.health_probe.probe_received
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 1 of the Health Probe journey: probe_received.
    note: |
      journey: health_probe
      journey_step: 1
      journey_role: root
      surface: node
      sentry_op: function
      sentry_span_name: "health_probe"
      impact: normal
      evidence: "span: http.server GET /healthz"
    attributes:
      - ref: health_probe.step
        requirement_level: required
        note: MUST be `probe_received` on this span.

  - id: span.health_probe.deps_checked
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 2 of the Health Probe journey: deps_checked.
    note: |
      journey: health_probe
      journey_step: 2
      journey_role: terminal
      surface: node
      sentry_op: function
      sentry_span_name: "health_probe.deps_checked"
      impact: normal
      evidence: "span: db SELECT 1"
    attributes:
      - ref: health_probe.step
        requirement_level: required
        note: MUST be `deps_checked` on this span.
