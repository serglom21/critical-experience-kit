# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Refund Request (refund_request)
#
# IMPORTANT: `requirement_level` on a SPAN group is documentation only.
# `weaver registry live-check` is sample-driven and has no span lookup —
# it will never report a missing span or a missing span attribute. Journey
# completeness is enforced by gap/analyze.py against Sentry. See
# BUILD-PLAN.md §0.
#
# span_kind and the `sentry_op` hints below are DEFAULTS derived from each
# step's `surface`. Nothing in a journey definition distinguishes an
# outbound call (client) from handling an inbound one (server) — review.

groups:

  - id: span.refund_request.refund_requested
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 1 of the Refund Request journey: refund_requested.
    note: |
      journey: refund_request
      journey_step: 1
      journey_role: root
      surface: browser
      sentry_op: ui.action
      sentry_span_name: "refund_request"
      impact: normal
    attributes:
      - ref: refund.id
        requirement_level: required
      - ref: refund_request.step
        requirement_level: required
        note: MUST be `refund_requested` on this span.

  - id: span.refund_request.refund_approved
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 2 of the Refund Request journey: refund_approved.
    note: |
      journey: refund_request
      journey_step: 2
      journey_role: step
      surface: node
      sentry_op: function
      sentry_span_name: "refund_request.refund_approved"
      impact: normal
    attributes:
      - ref: refund.id
        requirement_level: required
      - ref: refund_request.step
        requirement_level: required
        note: MUST be `refund_approved` on this span.

  - id: span.refund_request.refund_settled
    type: span
    span_kind: consumer
    stability: development
    brief: >-
      Step 3 of the Refund Request journey: refund_settled.
    note: |
      journey: refund_request
      journey_step: 3
      journey_role: terminal
      surface: worker
      sentry_op: queue.task
      sentry_span_name: "refund_request.refund_settled"
      impact: normal
    attributes:
      - ref: refund.id
        requirement_level: required
      - ref: refund_request.step
        requirement_level: required
        note: MUST be `refund_settled` on this span.
