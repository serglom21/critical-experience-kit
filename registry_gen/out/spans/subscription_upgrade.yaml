# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Subscription Upgrade (subscription_upgrade)
#
# IMPORTANT: `requirement_level` on a SPAN group is documentation only.
# `weaver registry live-check` is sample-driven and has no span lookup —
# it will never report a missing span or a missing span attribute. Journey
# completeness is enforced by gap/analyze.py against Sentry. See
# BUILD-PLAN.md §0.
#
# span_kind and the `sentry_op` hints below are DEFAULTS derived from each
# step's `surface`. Nothing in a journey definition distinguishes an
# outbound call (client) from handling an inbound one (server) — review.

groups:

  - id: span.subscription_upgrade.plan_selected
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 1 of the Subscription Upgrade journey: plan_selected.
    note: |
      journey: subscription_upgrade
      journey_step: 1
      journey_role: root
      surface: browser
      sentry_op: ui.action
      sentry_span_name: "subscription_upgrade"
      impact: normal
    attributes:
      - ref: upgrade.id
        requirement_level: required
      - ref: subscription_upgrade.step
        requirement_level: required
        note: MUST be `plan_selected` on this span.
      - ref: upgrade.outcome
        requirement_level:
          conditionally_required: >-
            Required before this span ends.

  - id: span.subscription_upgrade.payment_authorized
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 2 of the Subscription Upgrade journey: payment_authorized.
    note: |
      journey: subscription_upgrade
      journey_step: 2
      journey_role: step
      surface: node
      sentry_op: function
      sentry_span_name: "subscription_upgrade.payment_authorized"
      impact: normal
    attributes:
      - ref: upgrade.id
        requirement_level: required
      - ref: subscription_upgrade.step
        requirement_level: required
        note: MUST be `payment_authorized` on this span.

  - id: span.subscription_upgrade.entitlement_granted
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 3 of the Subscription Upgrade journey: entitlement_granted.
    note: |
      journey: subscription_upgrade
      journey_step: 3
      journey_role: terminal
      surface: node
      sentry_op: function
      sentry_span_name: "subscription_upgrade.entitlement_granted"
      impact: normal
    attributes:
      - ref: upgrade.id
        requirement_level: required
      - ref: subscription_upgrade.step
        requirement_level: required
        note: MUST be `entitlement_granted` on this span.
