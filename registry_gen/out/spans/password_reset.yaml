# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Password Reset (password_reset)
#
# IMPORTANT: `requirement_level` on a SPAN group is documentation only.
# `weaver registry live-check` is sample-driven and has no span lookup —
# it will never report a missing span or a missing span attribute. Journey
# completeness is enforced by gap/analyze.py against Sentry. See
# BUILD-PLAN.md §0.
#
# span_kind and the `sentry_op` hints below are DEFAULTS derived from each
# step's `surface`. Nothing in a journey definition distinguishes an
# outbound call (client) from handling an inbound one (server) — review.

groups:

  - id: span.password_reset.reset_requested
    type: span
    span_kind: internal
    stability: development
    brief: >-
      Step 1 of the Password Reset journey: reset_requested.
    note: |
      journey: password_reset
      journey_step: 1
      journey_role: root
      surface: browser
      sentry_op: ui.action
      sentry_span_name: "password_reset"
      impact: normal
      evidence: "src/auth/ResetRequest.tsx"
    attributes:
      - ref: password_reset.step
        requirement_level: required
        note: MUST be `reset_requested` on this span.

  - id: span.password_reset.email_sent
    type: span
    span_kind: consumer
    stability: development
    brief: >-
      Step 2 of the Password Reset journey: email_sent.
    note: |
      journey: password_reset
      journey_step: 2
      journey_role: step
      surface: worker
      sentry_op: queue.task
      sentry_span_name: "password_reset.email_sent"
      impact: normal
      evidence: "workers/mail.ts"
    attributes:
      - ref: password_reset.step
        requirement_level: required
        note: MUST be `email_sent` on this span.

  - id: span.password_reset.password_changed
    type: span
    span_kind: server
    stability: development
    brief: >-
      Step 3 of the Password Reset journey: password_changed.
    note: |
      journey: password_reset
      journey_step: 3
      journey_role: terminal
      surface: node
      sentry_op: function
      sentry_span_name: "password_reset.password_changed"
      impact: normal
      evidence: "app/api/auth/reset/route.ts"
    attributes:
      - ref: password_reset.step
        requirement_level: required
        note: MUST be `password_changed` on this span.
