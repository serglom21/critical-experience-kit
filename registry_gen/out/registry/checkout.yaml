# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Checkout (checkout)
#
# Attributes may only be DEFINED in an `attribute_group` whose id starts
# with `registry.`. The old `prefix:` field is rejected, so ids are full.
# `stability` is not required on an attribute_group.

groups:
  - id: registry.checkout
    type: attribute_group
    brief: Attributes describing the Checkout critical experience.
    attributes:
      - id: checkout.id
        type: string
        stability: development
        brief: >-
          Unique identifier for one journey instance. Carried on every span in the
          journey.
        note: >-
          Journey correlation key. MUST persist across: page_load, service. Not the
          trace ID — a browser navigation starts a new trace, so the trace ID cannot
          stitch a multi-page journey.
        examples: ["REPLACE_WITH_A_REAL_EXAMPLE"]
      - id: checkout.step
        type:
          members:
            - id: cart_reviewed
              value: "cart_reviewed"
              stability: development
              brief: "Step 1 of the journey."
            - id: shipping_submitted
              value: "shipping_submitted"
              stability: development
              brief: "Step 2 of the journey."
            - id: payment_submitted
              value: "payment_submitted"
              stability: development
              brief: "Step 3 of the journey."
            - id: payment_authorized
              value: "payment_authorized"
              stability: development
              brief: "Step 4 of the journey."
            - id: inventory_reserved
              value: "inventory_reserved"
              stability: development
              brief: "Step 5 of the journey."
            - id: order_created
              value: "order_created"
              stability: development
              brief: "Step 6 of the journey."
            - id: confirmation_viewed
              value: "confirmation_viewed"
              stability: development
              brief: "Step 7 of the journey."
        stability: development
        brief: >-
          The journey step this span represents.
        note: >-
          Enumerable and ordered — this enum is what makes drop-off computable.
      - id: checkout.outcome
        type:
          members:
            - id: completed
              value: "completed"
              stability: development
              brief: "Success."
            - id: failed
              value: "failed"
              stability: development
              brief: "Non-success terminal state."
            - id: abandoned
              value: "abandoned"
              stability: development
              brief: "Non-success terminal state."
            - id: rejected
              value: "rejected"
              stability: development
              brief: "Non-success terminal state."
        stability: development
        brief: >-
          Terminal outcome of the journey instance.
        note: >-
          A string enum, deliberately not a boolean: Sentry surfaces boolean
          attributes as the strings 'true'/'false', and a binary collapses distinct
          non-success states that route to different teams. Initialise to `abandoned`
          at journey start and overwrite on a terminal event, so an abandoned journey
          is distinguishable from an uninstrumented one.
      - id: payment.decline_reason
        type: string
        stability: development
        brief: >-
          Coded reason for a non-success outcome.
        note: >-
          MUST be a coded reason, never a free-text message and never a raw upstream
          payload. Keep cardinality low enough to be useful in a Group By.
        examples: ["insuffic_funds", "do_not_honor", "3ds_required", "gateway_timeout"]
      - id: cart.value
        type: double
        stability: development
        brief: >-
          Business measure attached at the value-bearing step.
        note: >-
          Numeric so it is chartable in Trace Explorer with no setup (`p50()`,
          `sum()`). A stringified value cannot be aggregated. Unit:
          major_currency_unit.
        examples: [1.0, 2.5]
      - id: order.value
        type: double
        stability: development
        brief: >-
          Business measure attached at the value-bearing step.
        note: >-
          Numeric so it is chartable in Trace Explorer with no setup (`p50()`,
          `sum()`). A stringified value cannot be aggregated. Unit:
          major_currency_unit.
        examples: [1.0, 2.5]
      - id: user.plan_tier
        type: string
        stability: development
        brief: >-
          Segmentation axis for slicing the journey.
        note: >-
          Turns 'the journey is slow' into 'the journey is slow for this segment'.
          Already available in the auth or tenancy layer.
        examples: ["REPLACE_WITH_A_REAL_EXAMPLE"]
      - id: payment.method
        type: string
        stability: development
        brief: >-
          Segmentation axis for slicing the journey.
        note: >-
          Turns 'the journey is slow' into 'the journey is slow for this segment'.
        examples: ["REPLACE_WITH_A_REAL_EXAMPLE"]
