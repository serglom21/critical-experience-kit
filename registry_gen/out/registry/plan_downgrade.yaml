# GENERATED by registry_gen/generate.py — do not edit by hand.
# Journey: Plan Downgrade (plan_downgrade)
#
# Attributes may only be DEFINED in an `attribute_group` whose id starts
# with `registry.`. The old `prefix:` field is rejected, so ids are full.
# `stability` is not required on an attribute_group.

groups:
  - id: registry.plan_downgrade
    type: attribute_group
    brief: Attributes describing the Plan Downgrade critical experience.
    attributes:
      - id: subscription.id
        type: string
        stability: development
        brief: >-
          Unique identifier for one journey instance. Carried on every span in the
          journey.
        note: >-
          Journey correlation key. MUST persist across: the journey. Not the trace ID
          — a browser navigation starts a new trace, so the trace ID cannot stitch a
          multi-page journey.
        examples: ["REPLACE_WITH_A_REAL_EXAMPLE"]
      - id: plan_downgrade.step
        type:
          members:
            - id: downgrade_requested
              value: "downgrade_requested"
              stability: development
              brief: "Step 1 of the journey."
            - id: proration_calculated
              value: "proration_calculated"
              stability: development
              brief: "Step 2 of the journey."
            - id: entitlement_revoked
              value: "entitlement_revoked"
              stability: development
              brief: "Step 3 of the journey."
        stability: development
        brief: >-
          The journey step this span represents.
        note: >-
          Enumerable and ordered — this enum is what makes drop-off computable.
